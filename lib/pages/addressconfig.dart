import 'package:flutter/cupertino.dart';

class Addressconfig extends StatelessWidget {
  const Addressconfig({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("config"),
    );
  }
}
